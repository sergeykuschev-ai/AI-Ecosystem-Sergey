# Copy to a server-only environment file outside Git, or set these values in
# the PowerShell session that runs the production check. Never commit secrets.
PURCHASING_API_TOKEN=replace-with-the-Arthur-Core-API-token
PURCHASING_BUILD_SHA=replace-with-git-rev-parse-short-HEAD
